# GENERATED by scripts/hpc/gen_civilcomments_budget_ladder.py -- do not
# hand-edit; change the generator and re-run. Derived from the frozen
# civilcomments_aggregate_500k_v1_gemma_large_HPC.yaml with paper-faithful v2 knobs (block_size 30, k_max 5,
# few_shot_probability 0.5, eval_pairs 40, max_blocks 8) at the 7500k_v2
# token budget. See the generator docstring for the full rationale.
# Aggregate CivilComments-WILDS large-held-out Dtest results -- 7500k v2
# (Gemma-2-27B), Rocket layout.

base: outputs/hpc/evaluation_large/seed_{seed}
seeds: [0]
output_dir: outputs/experiment_table/civilcomments_gemma_hpc_7500k_v2_large_seed0

methods:
  - name: FairCAPO
    eval_subdir: civilcomments_faircapo_7500k_v2_gemma
  - name: MO-CAPO (fairness off)
    eval_subdir: civilcomments_ablation_7500k_v2_gemma
  - name: NSGA-II-PO + fairness
    eval_subdir: civilcomments_nsga2po_7500k_v2_gemma
